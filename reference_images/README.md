# Reference Images

Put input images for `--image` here.

Use all images in this folder with:

```powershell
python .\generate_image.py --prompt-file .\prompt.txt --image .\reference_images --size 1024x1024 --verbose
```

Supported image types: `.png`, `.jpg`, `.jpeg`, `.webp`.

Images in this folder are ignored by Git by default.
